name: Get PR Number
description: "Get PR Number"

inputs:
  branch_name:
    description: "Branch Name"
    required: true
  after_pr_array:
    description: "List of PRs"
    required: true

runs:
  using: composite

  # Set the steps
  steps:
    - id: pr-num
      shell: bash
      env:
        BRANCH: ${{ inputs.branch_name }}
        PR_ARRAY: ${{ inputs.after_pr_array }}
      run: |
        echo "after_pr_number=$(node get_pr_number.js)" >> $GITHUB_OUTPUT
      working-directory: "${{ github.action_path }}"
